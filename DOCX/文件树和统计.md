# 项目文件树和统计报告

## 一、项目结构树

```
codex_register/
│
├── 📁 DOCX/                              # 文档目录（新建）
│   ├── 项目分析文档.md                     # 项目完整分析
│   ├── 代码对应关系文档.md                 # 代码功能映射
│   ├── 快速参考手册.md                     # 使用快速参考
│   └── 文件树和统计.md                     # 本文件
│
├── 📁 src/                                # 源代码目录
│   │
│   ├── 📄 index.ts                        # 主入口（~800 行）
│   │   └── 功能：命令行解析、流程编排、模式切换
│   │
│   ├── 📄 openai.ts                       # OpenAI 客户端（~1500+ 行）
│   │   └── 功能：Auth0 认证、OAuth 流程、Token 管理
│   │
│   ├── 📄 config.ts                       # 配置管理（~50 行）
│   │   └── 功能：加载和验证 config.json
│   │
│   ├── 📄 constants.ts                    # 常量定义（~100 行）
│   │   └── 功能：URL、超时、默认值等常量
│   │
│   ├── 📄 utils.ts                        # 工具函数（~200 行）
│   │   └── 功能：延迟、字符串处理、文件操作
│   │
│   ├── 📄 device-profile.ts               # 设备指纹生成（~150 行）
│   │   └── 功能：随机 User-Agent、屏幕、语言配置
│   │
│   ├── 📄 mailbox.ts                      # 邮箱配置（~80 行）
│   │   └── 功能：邮箱提供商选择和配置
│   │
│   ├── 📄 consume-hotmail.ts              # Hotmail 卡密消费（~100 行）
│   │   └── 功能：消费和记录已使用的卡密
│   │
│   ├── 📄 cliproxyapi.ts                  # CPA 接口封装（~200 行）
│   │   └── 功能：CliproxyAPI HTTP 请求
│   │
│   ├── 📄 cpa-codex.ts                    # CPA Codex 集成（~250 行）
│   │   └── 功能：OAuth URL、回调处理、文件下载
│   │
│   ├── 📄 probe-trial.ts                  # 试用探测（~150 行）
│   │   └── 功能：检查 ChatGPT 试用资格
│   │
│   ├── 📄 enroll-totp.ts                  # TOTP 注册（~100 行）
│   │   └── 功能：双因素认证注册
│   │
│   ├── 📄 batch-register.ts               # 批量注册（~200 行）
│   │   └── 功能：并发控制、失败重试、结果统计
│   │
│   ├── 📄 check-auth-quota.ts             # 账户配额检查（~100 行）
│   │   └── 功能：检查 OpenAI 账户配额
│   │
│   ├── 📄 check-plus.ts                   # Plus 订阅检查（~80 行）
│   │   └── 功能：验证 Plus 订阅状态
│   │
│   ├── 📄 sentinel.ts                     # Sentinel 核心（~300 行）
│   │   └── 功能：高级浏览器自动化逻辑
│   │
│   ├── 📄 sentinel-browser.ts             # Sentinel 浏览器（~200 行）
│   │   └── 功能：Playwright 浏览器集成
│   │
│   ├── 📄 sentinel-cli.ts                 # Sentinel CLI（~100 行）
│   │   └── 功能：命令行接口
│   │
│   ├── 📄 pw-skip-phone.ts                # Playwright 跳过手机验证（~150 行）
│   │   └── 功能：浏览器自动化辅助
│   │
│   ├── 📄 test-direct-oauth.ts            # 直接 OAuth 测试（~100 行）
│   │   └── 功能：测试直接 OAuth 流程
│   │
│   ├── 📄 test-skip-phone.ts              # 跳过手机验证测试（~80 行）
│   │   └── 功能：测试跳过手机验证
│   │
│   ├── 📄 test-skip-phone2.ts             # 跳过手机验证测试 2（~80 行）
│   │   └── 功能：测试跳过手机验证（变体）
│   │
│   ├── 📁 mail/                           # 邮箱模块目录
│   │   ├── 📄 hotmail.ts                  # Hotmail 处理（~400 行）
│   │   │   └── 功能：IMAP 连接、token 管理、OTP 提取
│   │   │
│   │   ├── 📄 gmail.ts                    # Gmail 处理（~200 行）
│   │   │   └── 功能：IMAP over TLS、应用专用密码
│   │   │
│   │   ├── 📄 cloudflare.ts               # Cloudflare 处理（~200 行）
│   │   │   └── 功能：Email Routing、临时邮箱
│   │   │
│   │   ├── 📄 gptmail.ts                  # GPTMail 处理（~200 行）
│   │   │   └── 功能：自建邮箱服务、API + IMAP
│   │   │
│   │   ├── 📄 proxiedmail.ts              # 代理邮箱处理（~150 行）
│   │   │   └── 功能：通过代理访问邮箱
│   │   │
│   │   ├── 📄 2925.ts                     # 2925 邮箱处理（~150 行）
│   │   │   └── 功能：特定邮箱提供商支持
│   │   │
│   │   ├── 📄 generate-email-name.ts      # 邮箱名生成（~80 行）
│   │   │   └── 功能：生成随机邮箱用户名
│   │   │
│   │   └── 📄 verification-matcher.ts     # 验证码匹配（~100 行）
│   │       └── 功能：从邮件内容提取 OTP
│   │
│   └── 📁 sms/                            # SMS 模块目录
│       ├── 📄 provider.ts                 # SMS 提供商接口（~50 行）
│       │   └── 功能：接口定义、类型声明
│       │
│       ├── 📄 heroSMS.ts                  # Hero SMS 实现（~200 行）
│       │   └── 功能：Hero SMS API 集成
│       │
│       ├── 📄 activation-broker.ts        # 号码租赁管理（~250 行）
│       │   └── 功能：号码池管理、重试逻辑
│       │
│       └── 📄 index.ts                    # 工厂方法（~50 行）
│           └── 功能：创建 SMSBroker 实例
│
├── 📄 config.example.json                 # 配置示例（~25 行）
│   └── 功能：展示所有可用配置选项
│
├── 📄 package.json                        # NPM 配置（~30 行）
│   └── 功能：依赖管理、脚本定义
│
├── 📄 tsconfig.json                       # TypeScript 配置（~20 行）
│   └── 功能：编译选项、模块配置
│
└── 📄 README.md                           # 项目说明（如果有）
    └── 功能：项目介绍、快速开始
```

---

## 二、文件统计

### 2.1 总体统计

| 指标 | 数值 |
|------|------|
| **总文件数** | 37 个 |
| **TypeScript 文件** | 32 个 |
| **JSON 配置文件** | 3 个 |
| **其他文件** | 2 个（tsconfig.json 等） |
| **总代码行数** | ~8000+ 行 |
| **模块数** | 5 个核心模块 |
| **文档数** | 4 个 |

### 2.2 按模块分类统计

| 模块 | 文件数 | 代码行数 | 占比 |
|------|--------|---------|------|
| **核心模块** | 5 | ~2000 | 25% |
| **邮箱模块** | 8 | ~1500 | 19% |
| **SMS 模块** | 4 | ~550 | 7% |
| **认证模块** | 4 | ~800 | 10% |
| **业务逻辑** | 6 | ~850 | 11% |
| **Sentinel 模块** | 3 | ~600 | 8% |
| **测试文件** | 3 | ~260 | 3% |
| **配置文件** | 3 | ~75 | 1% |
| **文档** | 4 | ~4000+ | 50% (文档) |
| **总计** | 37 | ~8000+ | 100% |

### 2.3 按文件类型统计

| 文件类型 | 数量 | 说明 |
|---------|------|------|
| `.ts` | 32 | TypeScript 源代码 |
| `.json` | 3 | 配置文件 |
| `.md` | 4 | 文档文件 |
| 其他 | 1 | tsconfig.json |

---

## 三、代码行数详情

### 3.1 核心模块行数

```
index.ts                    ~800 行
openai.ts                  ~1500 行
config.ts                   ~50 行
constants.ts                ~100 行
utils.ts                    ~200 行
device-profile.ts           ~150 行
mailbox.ts                  ~80 行
consume-hotmail.ts          ~100 行
cliproxyapi.ts              ~200 行
cpa-codex.ts                ~250 行
probe-trial.ts              ~150 行
enroll-totp.ts              ~100 行
batch-register.ts           ~200 行
check-auth-quota.ts         ~100 行
check-plus.ts               ~80 行
─────────────────────────────────────
核心模块小计                 ~4060 行
```

### 3.2 邮箱模块行数

```
hotmail.ts                  ~400 行
gmail.ts                    ~200 行
cloudflare.ts               ~200 行
gptmail.ts                  ~200 行
proxiedmail.ts              ~150 行
2925.ts                     ~150 行
generate-email-name.ts      ~80 行
verification-matcher.ts     ~100 行
─────────────────────────────────────
邮箱模块小计                 ~1480 行
```

### 3.3 SMS 模块行数

```
provider.ts                 ~50 行
heroSMS.ts                  ~200 行
activation-broker.ts        ~250 行
index.ts                    ~50 行
─────────────────────────────────────
SMS 模块小计                ~550 行
```

### 3.4 Sentinel 模块行数

```
sentinel.ts                 ~300 行
sentinel-browser.ts         ~200 行
sentinel-cli.ts             ~100 行
─────────────────────────────────────
Sentinel 模块小计           ~600 行
```

### 3.5 测试文件行数

```
test-direct-oauth.ts        ~100 行
test-skip-phone.ts          ~80 行
test-skip-phone2.ts         ~80 行
─────────────────────────────────────
测试文件小计                 ~260 行
```

### 3.6 辅助模块行数

```
pw-skip-phone.ts            ~150 行
─────────────────────────────────────
辅助模块小计                 ~150 行
```

---

## 四、依赖包统计

### 4.1 生产依赖 (7 个)

| 包名 | 版本 | 用途 |
|------|------|------|
| `playwright-core` | ^1.59.1 | 浏览器自动化 |
| `chromium-bidi` | ^15.0.0 | Chrome DevTools 协议 |
| `imapflow` | ^1.3.3 | IMAP 邮件处理 |
| `undici` | ^7.16.0 | HTTP 客户端 |
| `socks` | ^2.8.7 | SOCKS5 代理 |
| `tough-cookie` | ^5.1.1 | Cookie 管理 |
| `fetch-cookie` | ^3.1.0 | Fetch cookie 处理 |

### 4.2 开发依赖 (4 个)

| 包名 | 版本 | 用途 |
|------|------|------|
| `typescript` | ^5.9.2 | TypeScript 编译器 |
| `tsup` | ^8.5.1 | 打包工具 |
| `tsx` | ^4.19.2 | TypeScript 执行器 |
| `@types/node` | ^24.3.0 | Node.js 类型定义 |

---

## 五、功能模块依赖图

```
                    ┌─────────────────┐
                    │   index.ts      │
                    │   (主入口)      │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌──────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  config.ts   │  │   openai.ts      │  │  device-profile  │
│  (配置管理)  │  │  (OpenAI 客户端) │  │  (设备指纹)      │
└──────────────┘  └────────┬─────────┘  └──────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
┌──────────────┐  ┌──────────────────┐  ┌──────────────────┐
│   mail/      │  │     sms/         │  │  cpa-codex.ts    │
│  (邮箱模块)  │  │   (SMS 模块)     │  │  (CPA 集成)      │
└──────────────┘  └──────────────────┘  └──────────────────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                           ▼
                  ┌──────────────────┐
                  │    utils.ts      │
                  │   (工具函数)     │
                  └──────────────────┘
```

---

## 六、复杂度分析

### 6.1 圈复杂度评估

| 文件 | 复杂度 | 说明 |
|------|--------|------|
| index.ts | 高 | 多条件分支、复杂流程控制 |
| openai.ts | 高 | 多步骤认证流程、错误处理 |
| activation-broker.ts | 中 | 重试逻辑、状态管理 |
| hotmail.ts | 中 | IMAP 连接管理、错误恢复 |
| 其他文件 | 低 | 简单功能、工具函数 |

### 6.2 代码质量指标

| 指标 | 评估 |
|------|------|
| **可读性** | ✅ 良好 - 清晰的注释和日志 |
| **可维护性** | ✅ 良好 - 模块化设计 |
| **可测试性** | ⚠️ 中等 - 部分模块有测试 |
| **错误处理** | ✅ 良好 - 完善的错误捕获 |
| **日志记录** | ✅ 优秀 - 详细的标签日志 |

---

## 七、项目健康度

### 7.1 代码覆盖率

| 模块 | 测试覆盖 | 状态 |
|------|---------|------|
| 核心模块 | 30% | ⚠️ 需要改进 |
| 邮箱模块 | 20% | ⚠️ 需要改进 |
| SMS 模块 | 40% | ⚠️ 需要改进 |
| 认证模块 | 25% | ⚠️ 需要改进 |

### 7.2 依赖更新状态

| 依赖 | 当前版本 | 最新版本 | 状态 |
|------|---------|---------|------|
| playwright-core | ^1.59.1 | 最新 | ✅ |
| typescript | ^5.9.2 | 最新 | ✅ |
| imapflow | ^1.3.3 | 最新 | ✅ |

---

## 八、性能指标

### 8.1 执行时间估算

| 操作 | 预计时间 | 说明 |
|------|---------|------|
| 邮箱注册 | 30-60 秒 | 含 OTP 等待 |
| 手机注册 | 45-90 秒 | 含 SMS 接收 |
| CPA 流程 | 60-120 秒 | 含 OAuth 和入库 |
| 批量注册 (10个) | 5-10 分钟 | 串行执行 |

### 8.2 资源消耗

| 资源 | 消耗 | 说明 |
|------|------|------|
| 内存 | 100-200 MB | Playwright 浏览器 |
| CPU | 中等 | 浏览器自动化 |
| 网络 | 高 | 多次 HTTP 请求 |
| 磁盘 | 低 | 日志和 token 文件 |

---

## 九、维护建议

### 9.1 优先级排序

| 优先级 | 任务 | 说明 |
|--------|------|------|
| 🔴 高 | 增加单元测试 | 提高代码覆盖率 |
| 🟡 中 | 优化错误处理 | 统一错误处理机制 |
| 🟢 低 | 代码重构 | 减少函数复杂度 |
| 🟢 低 | 性能优化 | 并发控制改进 |

### 9.2 技术债务

1. **测试覆盖不足** - 需要补充单元测试和集成测试
2. **错误处理不一致** - 部分模块错误处理方式不同
3. **配置验证缺失** - 需要增加配置验证逻辑
4. **日志级别未分级** - 需要实现日志级别管理

---

## 十、版本历史

| 版本 | 日期 | 主要变更 |
|------|------|---------|
| 1.0.7 | 2026-06-14 | 当前版本 |
| 1.0.6 | - | CPA 集成优化 |
| 1.0.5 | - | SMS 模块改进 |
| 1.0.4 | - | 邮箱模块扩展 |
| 1.0.3 | - | 设备指纹优化 |
| 1.0.2 | - | 错误处理改进 |
| 1.0.1 | - | 性能优化 |
| 1.0.0 | - | 初始版本 |

---

*统计报告生成时间：2026-06-14*
*项目路径：A:\Github\codex_register*
*文档版本：1.0*